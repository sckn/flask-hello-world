pipeline {
    agent any
    stages {
        stage('Initialize'){
            steps {
                echo 'Initialize'
            }
        }
        stage('build') {
            steps {
                sh 'date'
            }
        }
    }
}
